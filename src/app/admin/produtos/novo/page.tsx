import {ProductForm} from "@/components/admin/ProductForm";import {requireProductManager} from "@/lib/auth";export default async function NewProduct(){await requireProductManager();return <main className="p-5 md:p-8"><p className="eyebrow">Catálogo</p><h1 className="mt-2 text-3xl font-black">Novo produto</h1><p className="mt-2 muted">O produto será publicado na Inevia.shop e associado ao armazém online.</p><ProductForm/></main>}
