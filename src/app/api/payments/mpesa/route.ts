import {NextResponse} from "next/server";import {z} from "zod";import {initiateMpesa} from "@/lib/payments/mpesa";const schema=z.object({phone:z.string().regex(/^\+?258\d{9}$/),amount:z.number().positive().max(1_000_000),reference:z.string().min(3).max(64)});export async function POST(request:Request){const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Pedido inválido."},{status:400});const result=await initiateMpesa(parsed.data);return NextResponse.json(result,{status:result.ok?200:503})}
