import type {MetadataRoute} from "next";import {getProducts} from "@/lib/commerce/products";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000";const products=await getProducts();return [{url:base,changeFrequency:"daily",priority:1},{url:`${base}/produtos`,changeFrequency:"daily",priority:.9},...products.map(p=>({url:`${base}/produto/${p.slug}`,changeFrequency:"weekly" as const,priority:.8}))]}
