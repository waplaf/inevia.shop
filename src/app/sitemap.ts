import type {MetadataRoute} from "next";import {demoProducts} from "@/lib/catalog";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000";return [{url:base,changeFrequency:"daily",priority:1},{url:`${base}/produtos`,changeFrequency:"daily",priority:.9},...demoProducts.map(p=>({url:`${base}/produto/${p.slug}`,changeFrequency:"weekly" as const,priority:.8}))]}
