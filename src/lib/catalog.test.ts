import {describe,expect,it} from "vitest";import {demoProducts,money} from "./catalog";describe("catalog",()=>{it("has unique friendly slugs",()=>expect(new Set(demoProducts.map(x=>x.slug)).size).toBe(demoProducts.length));it("formats metical prices",()=>expect(money(1000)).toContain("1"))});
