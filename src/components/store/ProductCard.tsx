import Image from "next/image";import Link from "next/link";import {money} from "@/lib/catalog";
type Product={slug:string;name:string;category:string;price:number;salePrice?:number;image:string;badge?:string};
export function ProductCard({p}:{p:Product}){return <article className="group card overflow-hidden"><Link href={`/produto/${p.slug}`}><div className="relative aspect-square overflow-hidden bg-slate-100"><Image src={p.image} alt={p.name} fill sizes="(max-width:768px) 50vw, 25vw" className="object-cover transition duration-500 group-hover:scale-105"/>{p.badge&&<span className="absolute left-3 top-3 rounded-full bg-[var(--green)] px-3 py-1 text-xs font-bold text-white">{p.badge}</span>}<button aria-label={`Adicionar ${p.name} aos favoritos`} className="absolute right-3 top-3 grid h-9 w-9 place-items-center rounded-full bg-white text-lg shadow">♡</button></div><div className="p-4"><p className="text-xs font-semibold text-[var(--green-dark)]">{p.category}</p><h3 className="mt-1 font-bold">{p.name}</h3><div className="mt-2 flex items-center gap-2"><strong className="text-lg text-[var(--green-dark)]">{money(p.salePrice??p.price)}</strong>{p.salePrice&&<del className="text-sm text-slate-400">{money(p.price)}</del>}</div></div></Link></article>}
