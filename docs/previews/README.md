# Previews do design

- `storefront.svg`: página inicial, catálogo/pesquisa, produto, carrinho, checkout e autenticação.
- `admin.svg`: dashboard, navegação administrativa, produtos, pedidos, métricas, atalhos e padrão usado pelas páginas CMS.

As pranchas são previews estáticos da linguagem visual implementada; não substituem screenshots de browser. Screenshots reais devem ser regenerados após `npm install`, migration/seed e `npm run dev`.
